{
  "name": "commandblock-desktop",
  "version": "0.1.0",
  "private": true,
  "description": "Tauri desktop shell for CommandBlock (wraps CommandBlock.API as a sidecar).",
  "scripts": {
    "publish:sidecar": "node scripts/publish-sidecar.mjs",
    "tauri": "tauri",
    "desktop:dev": "tauri dev",
    "desktop:build": "tauri build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
