{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capabilities for the CommandBlock desktop shell.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "updater:default",
    "dialog:default",
    "log:default",
    {
      "identifier": "shell:allow-execute",
      "allow": [
        {
          "name": "binaries/commandblock-api",
          "sidecar": true,
          "args": true
        }
      ]
    }
  ]
}
